info:
  name: Refresh Token
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
